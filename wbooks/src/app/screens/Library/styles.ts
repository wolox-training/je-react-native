import { StyleSheet } from 'react-native';
import { BACKGROUND_APP } from '@constants/colors';

export default StyleSheet.create({
  container: {
    backgroundColor: BACKGROUND_APP,
    alignItems: 'center'
  }
});
